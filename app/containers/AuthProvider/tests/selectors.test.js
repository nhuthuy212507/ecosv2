// import { selectAuthProviderDomain } from '../selectors';

describe('selectAuthProviderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
