import styled from 'styled-components';
import { Icon } from 'antd';

export default styled(Icon)`
  color: #1890ff;
  cursor: pointer;
  font-size: 16px;

  &:hover,
  &:focus {
    color: #40a9ff;
  }
`;
